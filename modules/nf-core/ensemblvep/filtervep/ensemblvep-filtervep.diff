Changes in component 'nf-core/ensemblvep/filtervep'
Changes in 'ensemblvep/filtervep/meta.yml':
--- modules/nf-core/ensemblvep/filtervep/meta.yml
+++ modules/nf-core/ensemblvep/filtervep/meta.yml
@@ -26,7 +26,7 @@
         pattern: "*.{vcf,tab,tsv,txt}"
         ontologies:
           - edam: http://edamontology.org/format_3475 # TSV
-  - feature_file:
+  - transcriptlist:
       type: file
       description: File containing features on separate lines. To be used with --filter
         option.

'modules/nf-core/ensemblvep/filtervep/environment.yml' is unchanged
Changes in 'ensemblvep/filtervep/main.nf':
--- modules/nf-core/ensemblvep/filtervep/main.nf
+++ modules/nf-core/ensemblvep/filtervep/main.nf
@@ -9,7 +9,7 @@
 
     input:
     tuple val(meta), path(input)
-    path feature_file
+    path transcriptlist
 
     output:
     tuple val(meta), path("*.${extension}"), emit: output
@@ -22,12 +22,13 @@
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}"
     extension = task.ext.suffix ?: "vcf"
+    def listfilter = transcriptlist ? "--filter \"Feature in $transcriptlist\"" : ""
     """
     filter_vep \\
         ${args} \\
         --input_file ${input} \\
-        --output_file ${prefix}.${extension} \\
-        --only_matched
+        --output_file ${prefix}.filt.${extension} \\
+        $listfilter
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
@@ -39,7 +40,7 @@
     def prefix = task.ext.prefix ?: "${meta.id}"
     extension = task.ext.suffix ?: "vcf"
     """
-    touch ${prefix}.${extension}
+    touch ${prefix}.filt.${extension}
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":

'modules/nf-core/ensemblvep/filtervep/tests/nextflow.config' is unchanged
'modules/nf-core/ensemblvep/filtervep/tests/main.nf.test' is unchanged
'modules/nf-core/ensemblvep/filtervep/tests/vcf.config' is unchanged
'modules/nf-core/ensemblvep/filtervep/tests/main.nf.test.snap' is unchanged
'modules/nf-core/ensemblvep/filtervep/tests/tab.gz.config' is unchanged
************************************************************
